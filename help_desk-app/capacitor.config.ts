import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'help_desk-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
